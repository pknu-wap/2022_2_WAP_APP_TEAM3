import 'package:flutter/material.dart';

class SearchPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title:Text("도서 검색 페이지"),
        backgroundColor: Color(0xff006285),),
      body: Container(),
    );
  }
}